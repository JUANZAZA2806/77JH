import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Origen77jhComponent } from './origen77jh.component';

describe('Origen77jhComponent', () => {
  let component: Origen77jhComponent;
  let fixture: ComponentFixture<Origen77jhComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Origen77jhComponent]
    });
    fixture = TestBed.createComponent(Origen77jhComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
