import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Viajerosjh77Component } from './viajerosjh77.component';

describe('Viajerosjh77Component', () => {
  let component: Viajerosjh77Component;
  let fixture: ComponentFixture<Viajerosjh77Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Viajerosjh77Component]
    });
    fixture = TestBed.createComponent(Viajerosjh77Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
