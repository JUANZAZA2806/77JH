import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Home77jhComponent } from './home77jh.component';

describe('Home77jhComponent', () => {
  let component: Home77jhComponent;
  let fixture: ComponentFixture<Home77jhComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Home77jhComponent]
    });
    fixture = TestBed.createComponent(Home77jhComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
