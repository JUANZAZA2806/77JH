import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Moscujh77Component } from './moscujh77.component';

describe('Moscujh77Component', () => {
  let component: Moscujh77Component;
  let fixture: ComponentFixture<Moscujh77Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Moscujh77Component]
    });
    fixture = TestBed.createComponent(Moscujh77Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
