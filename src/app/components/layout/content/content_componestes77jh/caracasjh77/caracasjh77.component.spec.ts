import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Caracasjh77Component } from './caracasjh77.component';

describe('Caracasjh77Component', () => {
  let component: Caracasjh77Component;
  let fixture: ComponentFixture<Caracasjh77Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Caracasjh77Component]
    });
    fixture = TestBed.createComponent(Caracasjh77Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
