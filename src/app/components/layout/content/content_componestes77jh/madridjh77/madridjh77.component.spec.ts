import { ComponentFixture, TestBed } from '@angular/core/testing';

import { Madridjh77Component } from './madridjh77.component';

describe('Madridjh77Component', () => {
  let component: Madridjh77Component;
  let fixture: ComponentFixture<Madridjh77Component>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [Madridjh77Component]
    });
    fixture = TestBed.createComponent(Madridjh77Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
